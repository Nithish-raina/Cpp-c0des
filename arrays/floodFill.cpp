#include <bits/stdc++.h>
#include <queue>
#include <utility>
int main(void)
{
	using std::queue;
	using std::cout;
	using std::pair;
	using std::endl;
	queue<pair<int,int>>q;
	q.push({1,2});
	q.push({3,4});
	q.push({5,6});
	return (1 != 0);
}
